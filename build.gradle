plugins {
    id 'java'
}

group 'org.mahanvafadaran'
version '1.0'
description 'kos amat'

repositories {
    mavenCentral()
    maven { url = 'https://hub.spigotmc.org/nexus/content/repositories/snapshots/' }
}

dependencies {
    compileOnly 'org.spigotmc:spigot-api:1.20.4-R0.1-SNAPSHOT'

}

test {
    useJUnitPlatform()
}